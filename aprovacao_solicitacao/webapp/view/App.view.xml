<mvc:View 
    controllerName="vicente.desafio5p.aprovacaosolicitacao.controller.App"
    displayBlock="true"
    xmlns:mvc="sap.ui.core.mvc" 
    xmlns="sap.m">
    
    <Shell id="_IDGenShell1">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>

</mvc:View>
